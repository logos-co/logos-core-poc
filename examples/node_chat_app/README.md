# Node.js FFI Example for Logos Core

This example demonstrates how to use the Logos Core library from Node.js using FFI (Foreign Function Interface).

WIP

requires symlink to `module_host` the moment
